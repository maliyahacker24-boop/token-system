{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "if [ -d frontend ]; then cd frontend && npm install; else npm install; fi",
  "buildCommand": "if [ -d frontend ]; then cd frontend && npm run build && cd .. && rm -rf dist && mkdir -p dist && cp -r frontend/dist/. dist/; else npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}