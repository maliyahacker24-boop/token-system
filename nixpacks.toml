[variables]
NODE_ENV = 'production'

[phases.setup]
nixPkgs = ['nodejs']

[phases.install]
cmds = ['cd backend && npm install']

[phases.build]
cmds = ['echo Backend build ready']

[start]
cmd = 'cd backend && npm start'